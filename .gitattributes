# Handle line endings automatically for files detected as text 
# and leave binary files untouched.
* text=auto

# Force LF for shell scripts (if any)
*.sh text eol=lf

# Force CRLF or LF based on auto-detection for JS/JSON
*.js text
*.jsx text
*.json text
*.css text
*.html text
